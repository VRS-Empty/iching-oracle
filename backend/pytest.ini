[pytest]
# scripts/ is on the path so the deploy helpers are importable under test —
# fetch_corpus runs during the Render build, where a silent bug is expensive.
pythonpath = . scripts
testpaths = tests
addopts = -q --tb=short
