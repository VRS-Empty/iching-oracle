# Production dependencies. Versions are pinned to what the test suite was
# actually run against — see README.md → Verifying a dependency bump.
flask==3.1.3
anthropic==0.120.0
httpx==0.28.1
numpy==2.4.6
gunicorn==23.0.0
